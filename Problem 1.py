class solution(object)
    def twoSum(self , numbers , target):
        self.numbers = numbers 
        self.target = target
        for solution in range(numbers):
            if solution[0] + solution[1] = target
               print([solution[0] + solution[1]])
            else:
                print("error")

numbers = [7 , 8 , 9]
target = 15
result = twoSum(numbers , target)